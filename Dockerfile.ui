FROM python:3.11-slim

WORKDIR /app

COPY requirements.ui.txt .
RUN pip install --no-cache-dir -r requirements.ui.txt

COPY ui/ ./ui/

CMD ["streamlit", "run", "ui/dashboard.py", "--server.port=8501", "--server.address=0.0.0.0", "--server.headless=true"]
